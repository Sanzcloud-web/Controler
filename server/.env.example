# Remote Controller Password
# Set this to secure your remote connection
REMOTE_PASSWORD=your-secret-password

# Tunnel mode: "local" (WiFi only), "cloudflared", or "ngrok"
TUNNEL_MODE=local

# For ngrok: set your auth token (get it from https://dashboard.ngrok.com)
# NGROK_AUTH_TOKEN=your-ngrok-token
